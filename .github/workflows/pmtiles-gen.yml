name: Generate PMTiles
on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - .github/workflows/pmtiles-gen.yml

jobs:
  generate:
    runs-on: [self-hosted, baltcircle-prod]
    timeout-minutes: 180
    permissions:
      contents: write
    steps:
      - name: Test runner
        run: echo "Runner OK at $(date)"
