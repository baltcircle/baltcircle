NODE_ENV=production
PORT=5000
DATABASE_PATH=/app/data/data.db

# Yandex Maps JS API key. Consumed by Vite at build time (import.meta.env).
# Leave empty to fall back to the stylized demo map.
VITE_YANDEX_MAPS_API_KEY=

